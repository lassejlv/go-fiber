<!DOCTYPE html>
<html>
  <head>
    <title>My First Web Page</title>
  </head>
  <body>
    <h1>Helo, {{ Name }}</h1>

    {{ len(users) }} users found

    {{ if len(users) == 2 }}
    <p>There are 2 users whohoohoho 🥳</p>
    {{ else }}
    <p>There are more or less users, than 2 💀</p>
    {{ end }}

    {{ range users }}
    <p>
      {{ .Name }}
      {{ .Age }}
    </p>
    {{ end }}
  </body>
</html>
